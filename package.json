{
  "name": "toimistobotti",
  "version": "1.0.0",
  "description": "The one and only, the legend, the disruptor, the all-seeing eye, the master, the bot.",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "watch": "tsc -w & nodemon -q -w dist dist/index.js",
    "build": "tsc --sourceMap false",
    "serve": "echo 'IT IS npm run watch, you moron...' && npm run watch",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lahdekorpi/toimistobotti.git"
  },
  "author": "",
  "license": "GPL-2.0-only",
  "bugs": {
    "url": "https://github.com/lahdekorpi/toimistobotti/issues"
  },
  "homepage": "https://github.com/lahdekorpi/toimistobotti#readme",
  "dependencies": {
    "@slack/web-api": "^6.1.0",
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mqtt": "^4.2.6",
    "typescript": "^4.2.4",
    "yaml": "^1.10.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/qs": "^6.9.6",
    "nodemon": "^2.0.7",
    "tslint": "^6.1.3"
  }
}
